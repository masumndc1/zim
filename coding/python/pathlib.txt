
...
... few examples of pathlib


from pathlib import Path

# Create a Path object (current directory)
p = Path(".")

# List all files in current directory
for file in p.iterdir():
    print(file)

# Create a new path
new_dir = Path("example_dir")

# Create the directory (if it doesn’t exist)
new_dir.mkdir(exist_ok=True)

# Work with files
file_path = new_dir / "hello.txt"         # Join paths with /
file_path.write_text("Hello, pathlib!")   # Write text to file

# Read the file content
content = file_path.read_text()
print("File content:", content)

# Check if path exists
if file_path.exists():
    print(f"{file_path} exists")

# Get absolute path
print("Absolute path:", file_path.resolve())
