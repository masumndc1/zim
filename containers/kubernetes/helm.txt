

... helm is the package manager for kubernetes
...
... install helm:
...
... brew install helm

curl https://baltocdn.com/helm/signing.asc | sudo apt-key add -
sudo apt-get install apt-transport-https --yes
echo "deb https://baltocdn.com/helm/stable/debian/ all main" |
      sudo tee /etc/apt/sources.list.d/helm-stable-debian.list
sudo apt-get update
sudo apt-get install helm

... add a repo:

helm repo add haproxytech https://haproxytech.github.io/helm-charts
helm repo update

... search for a repo and install
...

helm search repo haproxytech
NAME                          	CHART VERSION	APP VERSION	DESCRIPTION
haproxytech/haproxy           	1.9.0        	2.5.0      	A Helm chart for HAProxy on Kubernetes
haproxytech/kubernetes-ingress	1.18.1       	1.7.4      	A Helm chart for HAProxy Kubernetes Ingress Con...

helm install ingress --create-namespace ingress-controller haproxytech/kubernetes-ingress --version 1.18.1 -f values.yaml

... find out installed helm

masum@controller:~$ helm list --all-namespaces
NAME    NAMESPACE       REVISION        UPDATED STATUS  CHART   APP VERSION
masum@controller:~$

...
... delete a helm chart

masum@controller:~$ helm uninstall <helm_name> --namespaces <namespace>
masum@controller:~$ helm uninstall ingress-nginx --namespaces ingress-nginx

...
...

# Pull and unpack Cilium
helm pull cilium/cilium --version 1.16.1 --untar

# Pull and unpack Tetragon
helm pull cilium/tetragon --version 1.1.2 --untar

# Pull and unpack Kyverno
helm repo add kyverno https://kyverno.github.io/kyverno/
helm repo update
helm pull kyverno/kyverno --version 3.3.0 --untar

> helm upgrade --install cilium ./cilium \
  -n kube-system \
  --set ipam.mode=kubernetes \
  --set k8sServiceHost=kind-control-plane \
  --set k8sServicePort=6443 \
  --set kubeProxyReplacement=true \
  --set bpf.masquerade=true \
  --set operator.replicas=1 \
  --set cgroup.autoMount.enabled=true \
  --set bpf.autoMount.enabled=true

> helm upgrade --install kyverno ./kyverno \
  -n kyverno \
  --create-namespace \
  --set admissionController.replicas=1 \
  --set backgroundController.replicas=1 \
  --set cleanupController.replicas=1 \
  --set reportsController.replicas=1

NAME            NAMESPACE       REVISION        UPDATED                                 STATUS          CHART           APP VERSION
cilium          kube-system     1               2026-07-16 13:15:26.049634 +0300 EEST   deployed        cilium-1.16.1   1.16.1
kyverno         kyverno         1               2026-07-16 13:17:28.396294 +0300 EEST   deployed        kyverno-3.3.0   v1.13.0
tetragon        kube-system     1               2026-07-16 13:17:28.548426 +0300 EEST   deployed        tetragon-1.1.2  1.1.2
