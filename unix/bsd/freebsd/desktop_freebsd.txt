

... to have desktop in freebsd
... install gnome3 and xorg
... or install lxde-meta
... pkg install gnome3 xorg firefox-esr
... echo 'exec startlxde' > ~/.xinitrc
... to install lxde-meta firefox-esr xorg sudo

... for gnome do the following too

... /etc/fstab
proc           /proc       procfs  rw  0   0

... /etc/rc.conf

dbus_enable="YES"
hald_enable="YES"
gdm_enable="YES"
gnome_enable="YES"

... optionally we need to do the following

% echo "exec /usr/local/bin/gnome-session" > ~/.xinitrc
% echo "exec /usr/local/bin/gnome-session" > ~/.xsession


